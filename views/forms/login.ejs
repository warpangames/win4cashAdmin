<!-- const express = require('express');
const session = require('express-session');
const app = express();
// Configure session middleware
app.use(session({
secret: 'your-secret-key', // Replace with your own secret key
resave: false,
saveUninitialized: true,
}));

app.use(express.urlencoded({ extended: true })); // Middleware to parse form data

// Handle GET request to /form/login to render the login form
app.get('/form/login', (req, res) => {
res.render('forms/login'); // Render the login form view
});

// Handle POST request to /form/login
app.post('/form/login', (req, res) => {
const { username, password } = req.body;

// Replace this with your actual user validation logic
if (username === 'admin' && password === '123') {
req.session.admin = true;
req.session.sweet = 'Login is successful';
req.session.text = 'Hello, this is a demo admin';
res.redirect('/'); // Redirect to the home page or dashboard
} else {
res.redirect('/forms/login'); // Redirect back to the login page on failed login
}
}); -->

<!-- 


<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <script>
        alert("hii")
    </script>
</body>

</html> -->